package com.company.weeklyreport.security;

// Security infrastructure will be added in the JWT auth phase.
// Planned classes:
//   - JwtTokenProvider      (generate, validate, parse JWT)
//   - JwtAuthenticationFilter (OncePerRequestFilter for JWT extraction)
//   - UserDetailsServiceImpl  (loads UserDetails from UserRepository)
//   - SecurityConfig          (SecurityFilterChain, CORS, CSRF, role rules)
