package com.company.weeklyreport.entity;

/**
 * Role enum representing the two access levels in the system.
 * MEMBER — regular team member who submits weekly reports.
 * MANAGER — has elevated access to view team reports and the dashboard.
 */
public enum Role {
    MEMBER,
    MANAGER
}
